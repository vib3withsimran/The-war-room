services:
  - type: web
    name: the-war-room
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m server.main
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
